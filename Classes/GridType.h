#ifndef GRID_TYPE_H
#define GRID_TYPE_H

#include <string>
#include <vector>
#include <list>
#include <iostream>
#include <assert.h>

typedef enum
{
    BLANK,
    SNAKEBODY,
    FOOD,
    OBSTACLE

} GridType;
#endif
