#ifndef DIRECTION_H
#define DIRECTION_H

#include <string>
#include <vector>
#include <list>
#include <iostream>
#include <assert.h>

typedef enum
{
    UP,
    RIGHT,
    DOWN,
    LEFT

} Direction;
#endif
